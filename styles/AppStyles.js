import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1
  },
  tabBar: {
    flex: 0.1,
  },
  renderArea: {
    flex: 0.9,
    alignItems: 'center',
    justifyContent: 'center'
  }
})
